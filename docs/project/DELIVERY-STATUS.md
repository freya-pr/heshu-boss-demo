# 交付状态

> 更新日期：2026-08-24

| 能力 | 状态 | 说明 |
|---|---|---|
| Monorepo 与容器化 | 已实现 | Vue、Spring Boot、MySQL、Redis 由 Docker Compose 编排 |
| 双击演示启动 | 已实现并验证 | `启动SCRM演示版.command`（兼容脚本名），真实合数BOSS Vue 项目 + 本地演示数据 |
| 完整前后端启动 | 已配置，待 Docker 环境验证 | `启动SCRM完整前后端.command`（兼容脚本名） |
| 登录与 JWT | 已实现 | 账号密码真实校验，Redis 会话，企微扫码为明确 Stub |
| 动态菜单与权限 | 基础实现 | 管理员与一转老师菜单不同，系统管理接口后端鉴权 |
| V1.0 分级菜单与多页签 | 已实现并验证 | 固定左侧一级/二级菜单；页签去重、切换、关闭、当前页回退和会话恢复 |
| 首页—线索概览 | 已实现 | 首页模块仅保留线索概览，作为默认入口和固定页签；支持多维筛选、核心指标、转化漏斗、GMV退款、团队效能和下钻 |
| 线索到客户基础闭环 | 已实现并待本轮交互复验 | 引流线索取消人工新增和语音提醒；订单编号作为首要业务标识；支持筛选、员工分配、短信群发、按平台勾选顺序创建订单同步任务、固定模板导入解密数据、按当前列表导出非解密订单、活码加微回写、唯一客户转化和审计 |
| 客户中心 | 已实现基础闭环 | 手机号或 UnionID 创建；每个有效客户仅保存一个全局唯一主手机号；双身份冲突拦截；移除跟进记录和等级管理菜单；客户首次建档继承线索 S/A/B/C 等级，之后可独立编辑并留痕 |
| 业务提醒 | 已实现基础数据 | 业务、同步、加微和订单风险提醒由顶部通知入口承接，不设置独立菜单 |
| 个人中心 | 已实现可交互闭环 | 右上角个人卡片、基本资料、密码修改、企微唯一绑定说明及退出登录 |
| 系统管理 | 已实现基础交互 | 企业微信为唯一初始化来源，飞书不参与；账号与企微身份双向一对一；员工管理增加创建企微成员、邀请及待激活交互，并支持组织、员工、系统参数、字典类型及字典项维护 |
| 系统参数与字典后端底座 | 已提供接口与迁移 | 已增加参数、字典类型、字典项表及查询/新增/编辑接口；待 Java 环境完成编译、鉴权和集成测试 |
| 问卷、撞单 | 持续完善 | 当前 V1.0 菜单保留问卷管理和客户中心—撞单管理；撞单已纳入 V1.0 并具备演示交互及后端接口骨架。交付中心及营期管理整体移至 V2.0；订单中心、离职继承和班级管理已移除，不得误标为 V1.0 待交付功能 |
| 前端自动化校验 | 通过 | Vue 类型检查、Vite 生产构建、浏览器登录/菜单/分配/转客户验证通过，无控制台错误 |
| 后端自动化校验 | 待 Docker 环境 | 本机尚无 Java 和 Docker，须在技术环境执行 `mvn test` 及容器集成测试 |

## 当前结论

项目已从“静态 HTML 图”升级为可运行的 Vue 应用，产品和业务可以不依赖数据库直接验收页面与基础闭环。Spring Boot 后端、MySQL Flyway 脚本、Redis 会话及 Docker Compose 作为正式开发基线保留。
